# Dependencies / build output
node_modules/
dist/
release/
test-results/
*.log
.DS_Store

# Python bytecode cache (from the build scripts)
__pycache__/
*.pyc

# Local assistant / working notes — kept on disk, never published
.claude/
CLAUDE.md
docs/

# Large self-contained runtime bundles (~300 MB per platform). Shipped as
# release assets, not committed; reproducible from the build scripts under
# scripts/.
vendor/qemu-pebble-win/
vendor/pebble-py/
vendor/pebble-sdk/
vendor/qemu-pebble-linux/
vendor/pebble-py-linux/
vendor/qemu-pebble-mac/
vendor/pebble-py-mac/
